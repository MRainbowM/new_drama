
interface PeoplePageProps {
    params: {
        slug: string
    }
}

export default async function PeoplePage({ params: { slug } }: PeoplePageProps) {
    

    return (
        <>
        </>
    )
}