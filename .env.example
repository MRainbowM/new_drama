#
# Пример файла с переменными окружения
# Для использования:
# 1. Скопируйте этот файл в .env
# 2. Замените значения на свои
#

#
# PostgreSQL - настройки базы данных
#
# Пароль для подключения к БД (обязательно изменить в продакшене!)
POSTGRES_PASSWORD=postgres
# Имя пользователя БД
POSTGRES_USER=postgres
# Название базы данных
POSTGRES_DB=new_drama

#
# Frontend - настройки фронтенда
#
# URL для API запросов с фронтенда
# Для локальной разработки: http://localhost:8011/api
# Для продакшена: https://your-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:8011/api
API_URL=http://django-new-drama:8001/api
NEXT_PUBLIC_SITE_URL=http://django-new-drama:8011

# Базовый домен сайта
NEXT_PUBLIC_SITE_URL=http://localhost:8011

# Счетчик Яндекс метрики
NEXT_PUBLIC_YANDEX_METRIKA_ID=1111111111
# Верификация Яндекс Вебмастер
NEXT_PUBLIC_YANDEX_VERIFICATION=111111

#
# Nginx - основные настройки
#
# Порт на хост-машине (внешний порт)
# Для локальной разработки: 8011
# Для продакшена: 80
NGINX_HOST_PORT=8011

# Внутренний порт nginx в контейнере
NGINX_PORT=8011

# Порт для сертификатов
NGINX_HOST_PORT_SSL=443

# Доменное имя сервера
# Для локальной разработки: localhost
# Для продакшена: домен, например mysite.com
NGINX_SERVER_NAME=localhost

# URL для проксирования запросов
FRONTEND_URL=http://frontend-new-drama:3000
BACKEND_URL=http://django-new-drama:8001

#
# Django - настройки для локальной разработки
#
# Внешний порт для Django
DJANGO_HOST_PORT=8011
# Внутренний порт Django
DJANGO_PORT=8001

#
# Frontend - дополнительные настройки
#
# Внешний порт для фронтенда
FRONTEND_HOST_PORT=3001
# Внутренний порт фронтенда
FRONTEND_PORT=3001
# Настройка среды: 'production' | 'development'
NODE_ENV='development'


#
# Дополнительные настройки nginx (опционально)
#
# Путь к статическим файлам
NGINX_STATIC_PATH=/static
# Путь к медиа файлам
NGINX_MEDIA_PATH=/backend/media
# Максимальный размер загружаемых файлов
NGINX_CLIENT_MAX_BODY_SIZE=100M

# Расширенные настройки nginx (закомментированы, так как используются значения по умолчанию)
#NGINX_USER=root
#NGINX_WORKER_PROCESSES=1
#NGINX_WORKER_CONNECTIONS=1024
#NGINX_PROXY_CONNECT_TIMEOUT=70s
#NGINX_PROXY_SEND_TIMEOUT=86400
#NGINX_PROXY_READ_TIMEOUT=86400
#NGINX_SEND_TIMEOUT=86400
#NGINX_STATIC_AUTOINDEX=on

# ssl сертификаты
NGINX_SSL_CERT_PATH=/etc/letsencrypt/live/new-drama.ru/fullchain.pem
NGINX_SSL_KEY_PATH=/etc/letsencrypt/live/new-drama.ru/privkey.pem

